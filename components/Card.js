import React from 'react';
import styled from 'styled-components';

const Container = styled.View`
  width: 315px;
  height: 280px;
  border-radius: 14px;
  background-color: white;
  margin-top: 20px;
  margin-left: 20px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.15);
`;

const Card = porps => <Container />;

export default Card;
